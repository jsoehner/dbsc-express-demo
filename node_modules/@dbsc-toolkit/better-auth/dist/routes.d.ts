import { type StorageAdapter, type AnyTelemetryEvent } from "dbsc-toolkit";
import type { DbscCookies } from "./cookies.js";
export interface RouteDeps {
    /** Builds the storage bridge from a request ctx (ctx.context.adapter + internalAdapter). */
    storageFromCtx: (ctx: any) => StorageAdapter;
    cookies: DbscCookies;
    basePath: string;
    boundCookieTtl: number;
    /**
     * Mount the bound polyfill endpoints. Default true. When false, only the
     * two native endpoints are returned; the state endpoint still answers
     * `phase: "unbound"` so a client SDK stands down cleanly.
     */
    bound?: boolean;
    onEvent?: ((event: AnyTelemetryEvent) => void | Promise<void>) | undefined;
}
export declare function buildDbscRoutes(deps: RouteDeps): Record<string, unknown>;
//# sourceMappingURL=routes.d.ts.map